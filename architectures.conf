upstream instances {
  server architectures_api_1;
  server architectures_api_2;
}

server {
  listen 80;
  listen [::]:80;
  server_name localhost;

  location / {
    proxy_pass http://instances;
  }
}